<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shaba Parpia - AI Product Manager</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
            line-height: 1.6;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 1rem 0;
            transition: all 0.3s ease;
        }

        nav ul {
            display: flex;
            justify-content: center;
            list-style: none;
            gap: 2rem;
        }

        nav a {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            transition: color 0.3s ease;
            position: relative;
        }

        nav a:hover {
            color: #667eea;
        }

        nav a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 50%;
            background: #667eea;
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }

        nav a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 1px, transparent 1px);
            background-size: 50px 50px;
            animation: float 20s infinite linear;
        }

        @keyframes float {
            0% { transform: translateY(0px) rotate(0deg); }
            100% { transform: translateY(-50px) rotate(360deg); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
            color: white;
            animation: fadeInUp 1s ease-out;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #fff, #f0f8ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-subtitle {
            font-size: clamp(1.2rem, 2.5vw, 1.8rem);
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        .cta-button {
            display: inline-block;
            padding: 1rem 2rem;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
        }

        .cta-button:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        /* Sections */
        section {
            padding: 100px 0;
            background: white;
        }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 3rem;
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            width: 60px;
            height: 4px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 2px;
        }

        /* About Section */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 4rem;
            align-items: center;
        }

        .about-image {
            position: relative;
        }

        .about-image::before {
            content: '';
            position: absolute;
            top: 20px;
            left: 20px;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, #667eea, #764ba2);
            border-radius: 20px;
            z-index: -1;
        }

        .profile-img {
            width: 100%;
            max-width: 300px;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .profile-img:hover {
            transform: scale(1.05);
        }

        .about-text h3 {
            font-size: 1.5rem;
            color: #667eea;
            margin-bottom: 1rem;
        }

        .about-text p {
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            color: #666;
        }

        /* Skills Grid */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .skill-card {
            background: linear-gradient(135deg, #f8f9ff 0%, #f0f8ff 100%);
            padding: 2rem;
            border-radius: 20px;
            text-align: center;
            transition: all 0.3s ease;
            border: 1px solid rgba(102, 126, 234, 0.1);
        }

        .skill-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(102, 126, 234, 0.2);
        }

        .skill-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .skill-card h4 {
            font-size: 1.3rem;
            margin-bottom: 1rem;
            color: #333;
        }

        /* Projects Section */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
        }

        .project-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .project-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
        }

        .project-image {
            height: 200px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 4rem;
        }

        .project-content {
            padding: 2rem;
        }

        .project-content h4 {
            font-size: 1.4rem;
            margin-bottom: 1rem;
            color: #333;
        }

        .project-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            margin: 1rem 0;
        }

        .tag {
            background: #f0f8ff;
            color: #667eea;
            padding: 0.3rem 0.8rem;
            border-radius: 15px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        /* Contact Section */
        .contact-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .contact-content {
            text-align: center;
        }

        .contact-links {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 2rem;
            flex-wrap: wrap;
        }

        .contact-link {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: white;
            text-decoration: none;
            padding: 1rem 2rem;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50px;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .contact-link:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            nav ul {
                gap: 1rem;
            }

            .about-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .skills-grid {
                grid-template-columns: 1fr;
            }

            .projects-grid {
                grid-template-columns: 1fr;
            }

            .contact-links {
                flex-direction: column;
                align-items: center;
            }
        }

        /* Smooth Scrolling */
        html {
            scroll-behavior: smooth;
        }

        /* Loading Animation */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#skills">Skills</a></li>
            <li><a href="#projects">Projects</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <!-- Hero Section -->
    <section id="home" class="hero">
        <div class="hero-content">
            <h1>Shaba Parpia</h1>
            <p class="hero-subtitle">AI Product Manager & Innovation Strategist</p>
            <p style="font-size: 1.2rem; margin-bottom: 2rem; opacity: 0.8;">
                Transforming complex AI technologies into seamless user experiences
            </p>
            <a href="#about" class="cta-button">Explore My Work</a>
        </div>
    </section>

    <!-- About Section -->
    <section id="about">
        <div class="container">
            <h2 class="section-title fade-in">About Me</h2>
            <div class="about-content fade-in">
                <div class="about-image">
                    <div style="width: 300px; height: 300px; background: linear-gradient(45deg, #667eea, #764ba2); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-size: 4rem; margin: 0 auto;">
                        SP
                    </div>
                </div>
                <div class="about-text">
                    <h3>Bridging AI Innovation with Real-World Impact</h3>
                    <p>
                        With 5+ years of experience leading complex technology programmes, I specialize in transforming cutting-edge AI concepts into products that drive measurable business outcomes. My unique background spans government AI initiatives, quantum technologies, and cross-sector innovation.
                    </p>
                    <p>
                        At Digital Catapult, I've led the UK's first Quantum Technology Access Programme and delivered SONIC Labs initiatives, engaging 150+ stakeholders across technical and business functions. I bring a proven track record of 25% adoption increases and multi-million-pound programme delivery.
                    </p>
                    <p>
                        <strong>Currently seeking:</strong> Senior AI Product Manager roles where I can leverage my strategic vision, technical understanding, and stakeholder engagement expertise to drive AI transformation in financial services.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section id="skills">
        <div class="container">
            <h2 class="section-title fade-in">Core Competencies</h2>
            <div class="skills-grid">
                <div class="skill-card fade-in">
                    <div class="skill-icon">🤖</div>
                    <h4>AI Strategy & Vision</h4>
                    <p>Defining AI product roadmaps, identifying high-impact opportunities, and driving strategic AI adoption across complex organizations.</p>
                </div>
                <div class="skill-card fade-in">
                    <div class="skill-icon">📊</div>
                    <h4>Technical Product Management</h4>
                    <p>Leading cross-functional teams, managing product backlogs, and delivering technical products from concept to market.</p>
                </div>
                <div class="skill-card fade-in">
                    <div class="skill-icon">🎯</div>
                    <h4>Stakeholder Engagement</h4>
                    <p>Evangelizing product value, building consensus across technical and business stakeholders, and driving adoption.</p>
                </div>
                <div class="skill-card fade-in">
                    <div class="skill-icon">🔬</div>
                    <h4>User Research & Analytics</h4>
                    <p>Conducting user research, analyzing behavioral data, and translating insights into actionable product improvements.</p>
                </div>
                <div class="skill-card fade-in">
                    <div class="skill-icon">⚡</div>
                    <h4>Agile Development</h4>
                    <p>Implementing Design Thinking, Lean Startup, and Agile methodologies to accelerate product delivery and iteration.</p>
                </div>
                <div class="skill-card fade-in">
                    <div class="skill-icon">🚀</div>
                    <h4>Go-to-Market Strategy</h4>
                    <p>Developing adoption frameworks, benefit-realization models, and market readiness strategies for emerging technologies.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Projects Section -->
    <section id="projects">
        <div class="container">
            <h2 class="section-title fade-in">Featured Projects</h2>
            <div class="projects-grid">
                <div class="project-card fade-in">
                    <div class="project-image">
                        🔬
                    </div>
                    <div class="project-content">
                        <h4>UK's First Quantum Technology Access Programme</h4>
                        <p>Led strategic product development for quantum technology testbeds, coordinating multi-disciplinary teams and establishing new benchmarks for emerging tech adoption.</p>
                        <div class="project-tags">
                            <span class="tag">Product Strategy</span>
                            <span class="tag">Quantum Computing</span>
                            <span class="tag">Government Relations</span>
                        </div>
                        <p><strong>Impact:</strong> 100% milestone achievement, established national technology standards</p>
                    </div>
                </div>
                <div class="project-card fade-in">
                    <div class="project-image">
                        📡
                    </div>
                    <div class="project-content">
                        <h4>SONIC Labs AI-Driven Testbed Platform</h4>
                        <p>Developed comprehensive AI strategy for 5G and Open RAN testbed delivery, onboarding 10+ vendors and driving market adoption through intelligent automation.</p>
                        <div class="project-tags">
                            <span class="tag">5G Technology</span>
                            <span class="tag">AI Integration</span>
                            <span class="tag">Testbed Management</span>
                        </div>
                        <p><strong>Impact:</strong> Multi-million-pound programme delivery, 150+ stakeholder engagement</p>
                    </div>
                </div>
                <div class="project-card fade-in">
                    <div class="project-image">
                        🎯
                    </div>
                    <div class="project-content">
                        <h4>AI-Powered Accessibility Platform</h4>
                        <p>Product managed India's first on-demand sign language interpretation service, leveraging AI to enhance communication accessibility and user engagement.</p>
                        <div class="project-tags">
                            <span class="tag">AI Product Management</span>
                            <span class="tag">Accessibility</span>
                            <span class="tag">User Experience</span>
                        </div>
                        <p><strong>Impact:</strong> 25% adoption increase, 45% engagement improvement</p>
                    </div>
                </div>
                <div class="project-card fade-in">
                    <div class="project-image">
                        🧠
                    </div>
                    <div class="project-content">
                        <h4>Intelligent Driving Assistant</h4>
                        <p>Led development of AI-powered driving assistant for neurodivergent users, applying machine learning to create personalized, distraction-reducing experiences.</p>
                        <div class="project-tags">
                            <span class="tag">Machine Learning</span>
                            <span class="tag">User-Centered Design</span>
                            <span class="tag">Ford Partnership</span>
                        </div>
                        <p><strong>Impact:</strong> 10+ functional prototypes, senior stakeholder buy-in</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="contact-section">
        <div class="container">
            <div class="contact-content fade-in">
                <h2 class="section-title" style="color: white;">Let's Build the Future of AI Products</h2>
                <p style="font-size: 1.2rem; margin-bottom: 2rem;">
                    Ready to discuss how AI can transform your product strategy? Let's connect.
                </p>
                <div class="contact-links">
                    <a href="mailto:shabaparpia96@gmail.com" class="contact-link">
                        📧 Email Me
                    </a>
                    <a href="https://linkedin.com/in/shabaparpia" class="contact-link">
                        💼 LinkedIn
                    </a>
                    <a href="tel:+447526742035" class="contact-link">
                        📱 Call Me
                    </a>
                    <a href="#" class="contact-link" onclick="downloadCV()">
                        📄 Download CV
                    </a>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Fade in animation on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });

        // Navigation background change on scroll
        window.addEventListener('scroll', () => {
            const nav = document.querySelector('nav');
            if (window.scrollY > 50) {
                nav.style.background = 'rgba(255, 255, 255, 0.98)';
                nav.style.boxShadow = '0 2px 20px rgba(0, 0, 0, 0.1)';
            } else {
                nav.style.background = 'rgba(255, 255, 255, 0.95)';
                nav.style.boxShadow = 'none';
            }
        });

        // Download CV function
        function downloadCV() {
            alert('CV download functionality would be implemented here. For now, please contact me directly for my latest CV.');
        }

        // Dynamic typing effect for hero subtitle
        function typeEffect() {
            const texts = [
                'AI Product Manager & Innovation Strategist',
                'Transforming Complex AI into User-Friendly Products',
                'Driving AI Adoption Across Enterprise Ecosystems',
                'Building the Future of Intelligent Experiences'
            ];
            let textIndex = 0;
            let charIndex = 0;
            let isDeleting = false;
            const typeSpeed = 50;
            const deleteSpeed = 30;
            const pauseTime = 2000;

            function type() {
                const currentText = texts[textIndex];
                const subtitle = document.querySelector('.hero-subtitle');

                if (isDeleting) {
                    subtitle.textContent = currentText.substring(0, charIndex - 1);
                    charIndex--;
                } else {
                    subtitle.textContent = currentText.substring(0, charIndex + 1);
                    charIndex++;
                }

                let speed = isDeleting ? deleteSpeed : typeSpeed;

                if (!isDeleting && charIndex === currentText.length) {
                    speed = pauseTime;
                    isDeleting = true;
                } else if (isDeleting && charIndex === 0) {
                    isDeleting = false;
                    textIndex = (textIndex + 1) % texts.length;
                }

                setTimeout(type, speed);
            }

            setTimeout(type, 1000);
        }

        // Start typing effect when page loads
        window.addEventListener('load', typeEffect);
    </script>
</body>
</html>
